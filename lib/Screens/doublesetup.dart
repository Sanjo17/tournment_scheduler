import 'package:flutter/material.dart';

class Screen_doublesetup extends StatelessWidget {
  const Screen_doublesetup({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}